import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

const MartialArtsStudioGridSection = styled(Box)(({ theme }) => ({
  marginX: "auto",
  marginY: theme.spacing(4),
  textAlign: "center",
}));

export default MartialArtsStudioGridSection;
