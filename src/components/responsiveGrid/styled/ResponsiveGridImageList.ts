import { ImageList } from "@mui/material";
import { styled } from "@mui/material/styles";

export const ResponsiveGridImageList = styled(ImageList)({
  marginBlockStart: 0,
  marginBlockEnd: 0,
  marginInlineStart: 0,
  marginInlineEnd: 0,
});
