import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

const ResponsiveGridContainer = styled(Box)({
  padding: 10,
  maxWidth: "100vw",
});

export default ResponsiveGridContainer;
