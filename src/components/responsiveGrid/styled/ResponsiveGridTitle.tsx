import { Typography } from "@mui/material";
import { styled } from "@mui/material/styles";

const ResponsiveGridTitle = styled(Typography)({
  margin: `5px auto 5px 5px`,
  textAlign: "center",
});

export default ResponsiveGridTitle;
