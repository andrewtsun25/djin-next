import { styled } from "@mui/material/styles";
import { Box } from "@mui/material";

const EmploymentTypeSelectContainer = styled(Box)({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
});

export default EmploymentTypeSelectContainer;
