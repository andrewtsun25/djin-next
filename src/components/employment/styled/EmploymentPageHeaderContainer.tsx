import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

const EmploymentPageHeaderContainer = styled(Box)({
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
});

export default EmploymentPageHeaderContainer;
