import { Typography } from "@mui/material";
import { styled } from "@mui/material/styles";

const EmploymentPageTitle = styled(Typography)(({ theme }) => ({
  textAlign: "center",
  padding: theme.spacing(4),
}));

export default EmploymentPageTitle;
