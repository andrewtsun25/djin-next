"use client";

import { styled } from "@mui/material/styles";
import Image from "next/image";

export const EmploymentLogoImage = styled(Image)({
  maxWidth: 100,
  height: "auto",
  borderRadius: 50,
});
