"use client";

import { Card } from "@mui/material";
import { styled } from "@mui/material/styles";

const EmploymentCardContainer = styled(Card)(({ theme }) => ({
  margin: theme.spacing(2),
}));

export default EmploymentCardContainer;
