import { CardMedia } from "@mui/material";
import { styled } from "@mui/material/styles";

const EmploymentCardMedia = styled(CardMedia)({
  height: 0,
  paddingTop: "56.25%", // 16:9
});

export default EmploymentCardMedia;
