"use client";

import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

const HolisticOfficeLinkSectionContainer = styled(Box)(({ theme }) => ({
  width: "100%",
  marginBottom: theme.spacing(4),
}));

export default HolisticOfficeLinkSectionContainer;
