import { styled } from "@mui/material/styles";
import { Typography } from "@mui/material";

const HolisticOfficeLinkSectionTitle = styled(Typography)({
  margin: "30px auto",
  textAlign: "center",
});

export default HolisticOfficeLinkSectionTitle;
