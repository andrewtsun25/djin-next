"use client";

import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

export const HbvResearchCardList = styled(Box)(({ theme }) => ({
  marginTop: theme.spacing(4),
}));
