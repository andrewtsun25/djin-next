import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

const HbvResearchCardList = styled(Box)(({ theme }) => ({
  marginTop: theme.spacing(4),
}));

export default HbvResearchCardList;
