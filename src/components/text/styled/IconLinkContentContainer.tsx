import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

const IconLinkContentContainer = styled(Box)({
  display: "flex",
  alignItems: "center",
  margin: "1em 0",
});

export default IconLinkContentContainer;
