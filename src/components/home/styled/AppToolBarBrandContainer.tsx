import { Box } from "@mui/material";
import { styled } from "@mui/material/styles";

const AppToolBarBrandContainer = styled(Box)({
  height: "100%",
  display: "flex",
  alignItems: "center",
});

export default AppToolBarBrandContainer;
