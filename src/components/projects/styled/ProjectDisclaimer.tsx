import { Typography } from "@mui/material";
import { styled } from "@mui/material/styles";

const ProjectDisclaimer = styled(Typography)({
  fontWeight: "bold",
});

export default ProjectDisclaimer;
