"use client";

import { Typography } from "@mui/material";
import { styled } from "@mui/material/styles";

const ReclaimerTitle = styled(Typography)({
  textAlign: "center",
});

export default ReclaimerTitle;
