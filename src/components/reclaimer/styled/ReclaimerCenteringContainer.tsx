import { styled } from "@mui/material/styles";
import { Box } from "@mui/material";

const ReclaimerCenteringContainer = styled(Box)({
  display: "flex",
  justifyItems: "center",
  alignItems: "center",
  width: "100%",
});

export default ReclaimerCenteringContainer;
