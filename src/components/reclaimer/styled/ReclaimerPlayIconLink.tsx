import { styled } from "@mui/material/styles";

import { IconLink } from "../../text";

const ReclaimerPlayIconLink = styled(IconLink)({
  margin: "1em auto",
  color: "#ba4ef8",
});

export default ReclaimerPlayIconLink;
