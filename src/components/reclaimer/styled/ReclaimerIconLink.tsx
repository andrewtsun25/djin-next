import { styled } from "@mui/material/styles";
import { IconLink } from "../../text";

const ReclaimerIconLink = styled(IconLink)({
  margin: "0 auto",
  color: "#ba4ef8",
});

export default ReclaimerIconLink;
