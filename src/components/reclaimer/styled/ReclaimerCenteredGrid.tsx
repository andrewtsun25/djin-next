"use client";

import { Grid } from "@mui/material";
import { styled } from "@mui/material/styles";

const ReclaimerCenteredGrid = styled(Grid)({
  display: "flex",
  justifyItems: "center",
  alignItems: "center",
  width: "100%",
});

export default ReclaimerCenteredGrid;
