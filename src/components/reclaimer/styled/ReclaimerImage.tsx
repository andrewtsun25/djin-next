import Image from "next/image";
import { styled } from "@mui/material/styles";

const ReclaimerImage = styled(Image)({
  maxWidth: "100%",
  height: "auto",
  padding: 10,
  margin: "10px auto",
  border: "3px solid #ba4ef8",
  borderRadius: 10,
});

export default ReclaimerImage;
